# server {
#     listen 0.0.0.0:9999;  # Bad port
#     server_name test;
#     root /nonexistent/path/; # Bad path
#     error 404 /nonexistent/error.html;
    
#     location /test/ {
#         allow_method GET;
#     }
    
#     location /test/ {  # Duplicate
#         allow_method POST;
#     }
# }

# server {
#     listen 256.256.256.256:8080;  # Bad IP
#     server_name test2;
#     root www;
# }

server {
    listen 0.0.0.0:8080;  # Same as first server
    server_name test;  # Same server_name - potential conflict
    root www;
}

server {
    listen 0.0.0.0:8080;  # Same as previous server
    server_name test;  # Same server_name - potential conflict
    root www;
}
